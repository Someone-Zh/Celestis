---
name: fix
description: 用于修正已执行需求的缺陷或调整实现，基于 TDD 流程进行代码修复与测试验证。当用户提到"修复bug"、"修正需求"、"fix bug"、"修复问题"、"调整实现"、"代码修复"、"缺陷修复"、"fix requirement"、"修改已完成的功能"时自主唤醒。适用于需求已执行但存在问题需修正的场景，核心职责包括问题定位、失败测试编写、最小化修复、测试覆盖与文档更新。
version: 1.0.0
triggers:
  - type: command
    name: fix
globs:
  - ".spec/**/*"
  - "**/fix.md"
  - "**/design.md"
disable-model-invocation: false
user-invocable: true
argument-hint: "[需求目录路径] 或 [fix.md路径] 或 [需求描述]"
compatibility: "network: limited"
---
@Template/file_spec.md

@Template/base_root.md

@Template/base.md

## 1. 前置条件校验
* **必须** 通过给定的上下文信息能够明确推导出需求内容及对应的`{{Feature}}`目录或直接指定一个已存在的修正文件（`.spec/{{领域(Domain)}}/{{功能(Feature)}}/fix.md`）。
* 若无法精确定位需求目录，**必须**主动向用户询问获取后方可继续，严禁盲目执行。

## 2. 需求修正流水线（核心工作流）
1. **现有背景查阅：** 优先读取需求所属功能的规范文件（`.spec/{{领域(Domain)}}/domain.spec.md`），深入了解该功能目前涉及的架构设计与背景信息。
2. **执行修正：** 严格按照给定信息（`fix.md` 文件内容或对话中提出的修正诉求）去实现代码变更。
3. **更新任务清单：** 执行完毕后，必须追加更新`.archive` 下的最新版本目录中的 `changes/tasks.md` 文件，其内容格式**必须**严格遵守附录中的 `changes/tasks.md` 模板。
4. **外部变更记录：** 若修正过程中涉及外部依赖、配置或架构的变更，需生成对应的变更文件并写入到 `changes/{{变更的文件名.格式}}` 中。
5. **验收与测试隔离：** **必须**明确每个细分功能的验收标准，并根据该标准生成或调整对应的测试用例。对于与当前功能无关的外部依赖，必须进行 Mock（模拟阻断），测试文件存放位置应严格遵循项目规范或现有项目结构。
6. **质量红线：** 确保每一个新功能、新方法或被修改的代码都拥有完整的测试覆盖，编译与测试输出必须保持干净（零错误、零警告）。
7. **规范一致性：** 后续所有的持续对话与修正，都**必须严格**遵守当前规范，不得跑偏。
8. **改动痕迹追加：** 将修正引入的全新改动与设计调整，同样使用 `-------------` 分隔符追加到 `.archive` 下的最新版本目录中的 `design.md` 文件的尾部（**严禁修改历史信息**）。
9. **环境清理：** 若本次修正指定了临时的修正文件（`.spec/{{领域(Domain)}}/{{功能(Feature)}}/fix.md`），在上述流程全部执行完毕且验证通过后，**必须物理移除**该修正文件，保持工作区干净。
10. **版本稳定：** 修复仅是对最新版本的修正，**严禁**对历史版本进行修改已经变更当前版本号。

###  修正期必须坚持的核心理念
* **提问式推进：** 必须通过提问来完善粗略的想法，积极探索替代方案，并将设计拆分为不同章节逐步验证，及时保存设计文档。
* **微型任务拆解：** 将工作拆解为微小任务（每份控制在 2-5 分钟的执行量）。每个小任务都必须包含精确的文件路径、完整的代码实现以及明确的验证步骤。
* **强力践行 TDD（红-绿-重构）：** 必须强制执行 **RED-GREEN-REFACTOR** 循环：
  1. **红：** 先编写必定失败的测试用例（或能复现该 Bug 的失败测试），并亲眼观察到它失败。
  2. **绿：** 仅编写能够让该测试通过的最小限度代码，并亲眼观察到测试通过。
  3. **重构：** 在测试绿灯的保护下进行代码重构，并提交。
  *(铁律：严禁在没有失败测试支撑的情况下直接修改业务代码！)*
* **注释完整性：** 在用户没有明确要求“不写注释”的前提下，必须为新代码及受影响的旧代码添加完善、清晰、说明变更原因的注释信息。
* **存量测试同步：** 如果本次修正影响了已有的业务逻辑，**必须同步修改现有的存量测试用例**，确保老测试不会因为逻辑变更而挂掉。
* **分级审查机制：** 实施前必须进行计划审查，并按严重程度报告潜在问题。任何被定义为“关键（Critical）”的问题都将被视为阻碍（Blocker），必须立刻暂停推进并报告。
* **克制改动（Touch only what you must）：** 严禁扩大修改范围，只清理因你而起的“战场”。
  * **修改现有代码时：**
    1. 没坏的代码绝对不要重构。
    2. 没坏的代码绝对不要重构（重要的事情说两遍）。
    3. 必须保持并适配现有的代码风格，即使你个人并不喜欢。
  * **当你的修改产生“孤立残留”（Orphans）时：**
    1. 必须清理掉因**你的改动**而变成无用的 Import、变量、函数或类。
    2. 严禁清理由于历史原因遗留的死代码（除非用户明确要求）。
  * **最终检验标准：** 每一行被修改的代码，都必须能直接追溯到用户的具体需求。
* **可验证性：** 将任务转化为可验证的目标，定义成功指标，并循环迭代直至通过验证。
  1. “添加表单校验” $\rightarrow$ “为非法输入编写测试用例，然后编写代码使其通过”。
  2. “修复 Bug” $\rightarrow$ “编写一个复现该 Bug 的测试用例，然后修复它使其通过”。
  3. “重构 X 模块” $\rightarrow$ “确保重构前后，所有的既有测试用例全部通过”。

## 附录
* changes/tasks.md 文件格式
``` changes/tasks.md
@Template/task.md
```
