---
description: 执行需求
---
@Template/file_spec.md

@Template/base_root.md

@Template/base.md

## 1. 前置条件校验
* 执行前**必须**提供一个已存在的具体需求目录；若未提供或路径不明确，**必须**主动向用户询问获取后方可继续。

## 2. 需求执行流水线（核心工作流）
1. **上下文加载：** 优先读取需求所属功能的规范文件（`.spec/{{功能}}/spec.md`）以了解现有背景。若该文件不存在，则严格按照附录中的模板进行创建。
2. **读取设计：** 深入研读指定需求目录下的 `design.md` 文件，并严格根据其定义的内容与范围执行后续操作。
3. **生成任务清单：** 任务执行完毕后，必须在当前需求目录下生成 `changes/tasks.md` 文件，其内容格式**必须**严格遵守附录中的 `changes/tasks.md` 模板。
4. **外部变更记录：** 若开发过程中涉及外部依赖或配置的变更，需生成对应的变更文件并写入到 `changes/{{变更的文件名.格式}}` 中。
5. **规范一致性：** 后续所有的持续对话与修正，都**必须严格**遵守当前规范，不得跑偏。
6. **增量追加：** 将所有的全新改动使用 `-------------` 分隔符追加到 `design.md` 文件的尾部（**严禁擅自修改或覆盖历史信息**）。
7. **验收与测试隔离：** **必须**明确每个细分功能的验收标准，并根据该标准生成对应的测试用例。对于与当前功能无关的外部依赖，必须进行 Mock（模拟阻断），测试文件存放位置应严格遵循项目规范或现有项目结构。
8. **质量红线：** 确保每一个新功能/新方法都拥有完整的测试覆盖，编译与测试输出必须保持干净（零错误、零警告）。
9. **禁止越权更新：** 在本阶段**严禁**更新所属功能规范文件（`.spec/{{功能}}/spec.md`）中“当前功能详细信息”下的对应设计文档路径。此项工作统一留给后续的“归档阶段”进行分析、融合与修改。

### 编码期必须遵循的刚性规则
* **强力践行 TDD（红-绿-重构）：** 必须强制执行 **RED-GREEN-REFACTOR** 循环：
  1. **红：** 先编写必定失败的测试用例，并亲眼观察到它失败。
  2. **绿：** 仅编写能够让该测试通过的最小限度代码，并亲眼观察到测试通过。
  3. **重构：** 在测试绿灯的保护下进行代码重构，并提交。
  *(铁律：严禁编写任何没有对应失败测试支撑的业务代码！)*
* **注释完整性：** 在用户没有明确要求“不写注释”的前提下，必须为新代码添加完善、清晰、有业务前因后果的注释信息。
* **分级审查机制：** 实施前必须进行计划审查，并按严重程度报告潜在问题。任何被定义为“关键（Critical）”的问题都将被视为阻碍（Blocker），必须立刻暂停推进并报告。
* **克制改动（Touch only what you must）：** 严禁扩大修改范围，只清理因你而起的“战场”。
  * **修改现有代码时：**
    1. 没坏的代码绝对不要重构。
    2. 没坏的代码绝对不要重构（重要的事情说两遍）。
    3. 必须保持并适配现有的代码风格，即使你个人并不喜欢。
  * **当你的修改产生“孤立残留”（Orphans）时：**
    1. 必须清理掉因**你的改动**而变成无用的 Import、变量、函数或类。
    2. 严禁清理由于历史原因遗留的死代码（除非用户明确要求）。
  * **最终检验标准：** 每一行被修改的代码，都必须能直接追溯到用户的具体需求。
* **可验证性：** 将任务转化为可验证的目标，定义成功指标，并循环迭代直至通过验证。
  1. “添加表单校验” $\rightarrow$ “为非法输入编写测试用例，然后编写代码使其通过”。
  2. “修复 Bug” $\rightarrow$ “编写一个复现该 Bug 的测试用例，然后修复它使其通过”。
  3. “重构 X 模块” $\rightarrow$ “确保重构前后，所有的既有测试用例全部通过”。

## 3. 超大文件拆分原则
* 当某个设计文件过于庞大（达到 400 行左右）时，必须进行物理拆分：在 `design.md` 同级目录下创建 `{{功能点}}.md`，并在主 `design.md` 中以引用/链接的形式描述子功能点。
* 如果拆分出的 `{{功能点}}.md` 依然过于庞大，依此类推继续向下拆分，以实现架构的渐进式加载与阅读。

## 附录
* changes/tasks.md 文件格式
``` changes/tasks.md
@Template/task.md
```
* .spec/{{功能}}/spec.md 文件格式
``` .spec/{{功能}}/spec.md
@Template/function_spec.md
```
