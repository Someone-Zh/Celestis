---
name: apply
description: 用于执行已规划好的`功能(Feature)`设计方案，将设计文档转化为可运行的代码实现。当用户提到"执行需求"、"开始开发"、"实现设计"、"落实方案"、"apply design"、"执行设计文档"时自主唤醒。适用于`功能(Feature)`目录已明确、design.md 已就绪的场景，核心职责包括 TDD 开发、测试用例编写、代码实现与质量保障。
version: 1.0.0
triggers:
  - type: command
    name: apply
globs:
  - ".spec/**/*"
  - "**/design.md"
  - "**/spec.md"
disable-model-invocation: false
user-invocable: true
argument-hint: "[`功能(Feature)`目录路径]"
compatibility: "network: limited"
---
@Template/file_spec.md

@Template/base_root.md

@Template/base.md

## 1. 前置条件校验
* 执行前**必须**提供一个已存在的具体`.spec/{{领域(Domain)}}/{{功能(Feature)}}/design.md`文件，若未提供或路径不明确，**必须**主动向用户询问获取后方可继续。
* 阅读所属领域规范文件（`.spec/{{领域}}/domain.spec.md`）,了解当前领域的设计规范。
* 阅读设计方案（`.spec/{{领域(Domain)}}/{{功能(Feature)}}/design.md`）。
* 阅读任务清单（`.spec/{{领域(Domain)}}/{{功能(Feature)}}/changes/tasks.md`）。
## 2. 实施前动作
* 将当前`design.md` 文件和整个`changes`目录移入 同级的 `.archive` 目录下 `{{版本号}}` 目录内。（没有对应目录则创建,必须保证`.spec/{{领域(Domain)}}/{{功能(Feature)}}/`目录下只有`feature.spec.md`）。
* 如果 `feature.spec.md` 不存在，则按照下方的 .spec/{{领域(Domain)}}/{{功能(Feature)}}/feature.spec.md 文件格式创建文件。如果存在则根据本次改动去更新。
* 必须秉承有则更新无则新建的理念使`feature.spec.md`拥有全部的最新最全的设计信息。
* 后续如果继续沟通调整，则在执行前先根据内容更新已经移动到 `.archive` 目录下 `{{版本号}}` 目录内的 `design.md` 文件和 `changes` 目录下的文件，然后在执行改动，务必保证设计现行。

## 3. `功能(Feature)`执行流水线（核心工作流）
1. **读取设计：** 深入研读指定`功能(Feature)`目录下的 `design.md` 文件，并严格根据其定义的内容与范围执行后续操作。
2. **原子拆解：** 必须将宏观需求拆解为“2-5分钟即可完成/验证”的原子级微型任务。每次只推进一个微小任务，并形成任务列表动态向用户同步，严禁单次合并交付庞大复杂的改动。
3. **遵循任务清单：** 每次只推进一个微小任务，并形成任务列表动态向用户同步，严禁单次合并交付庞大复杂的改动。
4. **外部变更记录：** 若开发过程中涉及外部依赖或配置的变更，需生成对应的变更文件并写入到归档目录中的 `changes/{{变更的文件名.格式}}` 中。
5. **规范一致性：** 后续所有的持续对话与修正，都**必须严格**遵守当前规范，不得跑偏。
6. **增量追加：** 将所有的全新改动使用 `-------------` 分隔符追加到 `design.md` 文件的尾部（**严禁擅自修改或覆盖历史信息**）。
7. **验收与测试隔离：** **必须**明确每个细分功能的验收标准，并根据该标准生成对应的测试用例。对于与当前功能无关的外部依赖，必须进行 Mock（模拟阻断），测试文件存放位置应严格遵循项目规范或现有项目结构。
8. **质量红线：** 确保每一个新功能/新方法都拥有完整的测试覆盖，编译与测试输出必须保持干净（零错误、零警告）。
9. **禁止越权更新：** 在本阶段**严禁**更新所属领域规范文件（`.spec/{{领域}}/domain.spec.md`）中“当前功能详细信息”下的对应设计文档路径。此项工作统一留给后续的“归档阶段”进行分析、融合与修改。

### 编码期必须遵循的刚性规则
* **践行 TDD（红-绿-重构）：** 执行 **RED-GREEN-REFACTOR** 循环：*(铁律：严禁编写任何没有对应失败测试支撑的业务代码！)*
* **注释完整性：** 在用户没有明确要求“不写注释”的前提下，必须为新代码添加完善、清晰、有业务前因后果的注释信息。
* **运行可审计：** 在没有明确要求禁止日志的前提下，必须为所有的关键操作添加日志记录。
* **分级审查机制：** 实施前必须进行计划审查，并按严重程度报告潜在问题。任何被定义为“关键（Critical）”的问题都将被视为阻碍（Blocker），必须立刻暂停推进并报告。
* **克制改动（Touch only what you must）：** 严禁扩大修改范围，只清理因你而起的“战场”。
  * **修改现有代码时：**
    1. 没坏的代码绝对不要重构。
    2. 没坏的代码绝对不要重构（重要的事情说两遍）。
    3. 必须保持并适配现有的代码风格，即使你个人并不喜欢。
  * **当你的修改产生“孤立残留”（Orphans）时：**
    1. 必须清理掉因**你的改动**而变成无用的 Import、变量、函数或类。
    2. 严禁清理由于历史原因遗留的死代码（除非用户明确要求）。
  * **最终检验标准：** 每一行被修改的代码，都必须能直接追溯到用户的具体需求。
* **可验证性：** 将任务转化为可验证的目标，定义成功指标，并循环迭代直至通过验证。
  1. “添加表单校验” $\rightarrow$ “为非法输入编写测试用例，然后编写代码使其通过”。
  2. “修复 Bug” $\rightarrow$ “编写一个复现该 Bug 的测试用例，然后修复它使其通过”。
  3. “重构 X 模块” $\rightarrow$ “确保重构前后，所有的既有测试用例全部通过”。
* **坚持职责:** **必须**对给定的设计方案所有功能都严格实现，**必须**全部立即完成，**禁止**出现未完成的、mock的、不完整的功能,**禁止**完成部分功能，**禁止**后续再说。
* **任务清单驱动：** **必须**严格按照 `changes/tasks.md`（归档）中的任务清单逐一完成并维护好任务状态变更，不得跳过任何一个任务，也不得遗留任何一个任务。


## 4. 大文件拆分原则
* 当某个功能文件`feature.spec.md`过于庞大（达到 400 行左右）时，必须进行物理拆分：在 `feature.spec.md` 同级目录下创建 `feature.{{具体子功能}}.md`，并在主 `feature.spec.md` 中以引用/链接的形式描述并连接到该子功能点。
* 如果拆分出的 `feature.{{具体子功能}}.md` 依然过于庞大，依此类推继续向下拆分，以实现架构的渐进式加载与阅读。


## 附录
* changes/tasks.md 文件格式
``` changes/tasks.md
@Template/task.md
```
* .spec/{{领域(Domain)}}/{{功能(Feature)}}/feature.spec.md 文件格式
``` .spec/{{领域(Domain)}}/{{功能(Feature)}}/feature.spec.md 文件格式
@Template/feature_spec.md
```
