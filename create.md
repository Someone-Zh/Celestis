---
name: create
description: 用于创建新的功能需求设计方案，从用户意图到完整设计文档的规划工具。当用户提到"创建需求"、"新建功能"、"规划需求"、"设计新功能"、"create requirement"、"需求设计"、"功能规划"、"新需求"时自主唤醒。适用于从零开始规划功能的场景，核心职责包括需求收集、架构探索、设计文档编写与验收标准制定。
version: 1.0.0
triggers:
  - type: command
    name: create
globs:
  - ".spec/**/*"
disable-model-invocation: false
user-invocable: true
argument-hint: "[功能名称] [需求名称]"
compatibility: "network: limited"
---
@Template/file_spec.md

@Template/base_root.md

@Template/base.md

## 1. 从输入信息中分析领域与功能
* 如果输入提供的是标准需求目录 `.spec/{{领域}}/{{功能}}/design.md`（例如：`.spec/auth/login/design.md`），请直接从中提取需求内容以及初步的设计方案。
* 如果输入的目录不完整（例如仅有 `.spec/{{领域}}/{{功能}}` 或 `.spec/{{领域}}`），则根据已有信息进行追问。
* 如果给定信息为设计方案（文件或者直接提供），则直接分析方案自动划分或新建到相应的领域与功能（允许同时操作多个领域的多个功能）。
* 如果完全没有提供上下文或信息无法确定，必须进一步提问。
* **只有在完整获取到领域、功能、设计三要素后，才允许执行后续的“创建功能需求”流程。**

## 2. 功能索引校验
* 分析输入信息，并在 `.spec/spec.md` 尾部的` 业务领域（Domain）与特性（Feature）索引` 列表中查找是否存在对应领域与公功能。
* 若不存在，则根据当前信息进行创建（可能会有多个）。
* 注意：此步骤仅包含该功能的规范文件路径（如 `.spec/{{领域}}/domain.spec.md`），不要包含任何下级目录或文件。

## 3. 创建功能需求（核心工作流）

### 必须遵守的工作流程
1. **前置查阅：** 读取需求所属功能的规范文件（`.spec/{{领域}}/domain.spec.md`）以了解现有背景。若文件不存在，则严格按照附录中的模板进行创建（注意：规范文件不要重复包含全局的`spec.md`和`rules.md`的约定）。 
2. **充分收集：** 尽最大可能收集上下文，包括但不限于涉及的准确文档、当前代码位置、现有业务逻辑等，确保需求创建的准确性。
3. **意图探索：** 在正式实施前，深入探索用户的真实意图、潜在需求和架构设计。
4. **单点提问：** 每次只提出一到三个问题。**宁可增加沟通的对话轮数，也绝不毕其功于一役，避免单次问题过多给用户带来理解压力。**
5. **疑必确问：** 任何有疑问或模糊不清的地方，必须向用户确认具体意图后方可继续。**不要害怕因提问频繁而打扰用户，确保所有疑惑和潜在问题点在动手前100%对齐。**
6. **选项优先：** 优先提供“多项选择题”，而非“开放式简答题”，降低用户的沟通成本。
7. **渐进式验证：** 采用“展示设计 $\rightarrow$ 获得授权 $\rightarrow$ 继续推进”的循环模式。
8. **保持灵活性：** 发现设计中存在不合理之处时，随时与用户澄清。
9. **极简主义：** 无情地裁剪设计中所有非必要、冗余的“镀金”功能。
10. **明确指标：** 明确每一个功能点的具体验收标准（Acceptance Criteria）。
11. **原子拆解：** 必须将宏观需求拆解为“2-5分钟即可完成/验证”的原子级微型任务。必须根据设计在执行前生成任务清单，必须在当前`功能(Feature)`目录下生成 `changes/tasks.md` 文件，其内容格式**必须**严格遵守附录中的 `changes/tasks.md` 模板。
12. **外部变更记录：** 若涉及外部依赖或配置的变更，需生成对应的变更文件并写入到 `changes/{{变更的文件名.格式}}` 中（比如数据库的变更文件 `changes/20240812123456_create_table.sql`）。


### 必须坚持的核心理念
* **严禁盲目猜测（Don't assume）：** 严禁隐瞒疑惑。必须将潜在的权衡利弊（Trade-offs）摆在台面上。在实施前：
  1. 显式阐述你的假设。如有不确定，立即提问。
  2. 若存在多种实现方案，列出并对比它们，绝不悄悄做决定。
  3. 如果有更简单的替代方案，请主动提出。在不合理的需求面前，要敢于据理力争（Push back）。
  4. 一旦出现含糊不清的概念，立刻停下，指出困惑点并寻求澄清。
* **克制改动（Touch only what you must）：** 严禁扩大修改范围，只清理因你而起的“战场”。
  * **修改现有代码时：**
    1. 没坏的代码绝对不要重构。
    2. 没坏的代码绝对不要重构（重要的事情说两遍）。
    3. 必须保持并适配现有的代码风格，即使你个人并不喜欢。
  * **当你的修改产生“孤立残留”（Orphans）时：**
    1. 必须清理掉因**你的改动**而变成无用的 Import、变量、函数或类。
    2. 严禁清理由于历史原因遗留的死代码（除非用户明确要求）。
  * **最终检验标准：** 每一行被修改的代码，都必须能直接追溯到用户的具体需求。
* **可验证性：** 将任务转化为可验证的目标，定义成功指标，并循环迭代直至通过验证。
  1. “添加表单校验” $\rightarrow$ “为非法输入编写测试用例，然后编写代码使其通过”。
  2. “修复 Bug” $\rightarrow$ “编写一个复现该 Bug 的测试用例，然后修复它使其通过”。
  3. “重构 X 模块” $\rightarrow$ “确保重构前后，所有的既有测试用例全部通过”。
* **坚持职责:** 当前仅做设计文档`design.md`的编写和领域与规范文件(`domain.spec.md`和`.spec/spec.md`)的创建或更新，不做代码的修改和其他任何文件的创建。

  
## 4. 产物交付与持久化
* `.spec/{{领域(Domain)}}/{{功能(Feature)}}/feature.spec.md`文件如果存在且没有明确指定版本号规则时，则根据其中的版本号变更规则，在`design.md`明确本次变更后的版本号。
* 如果需要新建 `.spec/{{领域}}/domain.spec.md` 并且设计文档没给出版本号，则初始化V1最为本次版本号。
* 将最终评审通过的功能需求规划与设计，写入到对应的需求设计文件（`.spec/{{领域}}/{{功能}}/design.md`）中。
* 必须要有和设计文件对应的任务清单`changes/tasks.md`。
* 设计文件（`.spec/{{领域}}/{{功能}}/design.md`）必须满足全局和领域的规范要求。
* 设计文件（`.spec/{{领域}}/{{功能}}/design.md`）必须做到仅凭这个设计文档就能完成本次开发。
* 后续的对话必须严格基于该规范执行，并根据沟通进展持续迭代和修改该 `design.md` 文件。


## 附录
* changes/tasks.md 文件格式
``` changes/tasks.md
@Template/task.md
```
* .spec/{{领域}}/domain.spec.md 文件格式
``` .spec/{{领域}}/domain.spec.md
@Template/domain_spec.md
```
* .spec/{{领域(Domain)}}/{{功能(Feature)}}/feature.spec.md 文件格式
``` .spec/{{领域(Domain)}}/{{功能(Feature)}}/feature.spec.md 文件格式
@Template/feature_spec.md
```
