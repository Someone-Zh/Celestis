# {{Feature当前功能名称}} 特性全量规范 (feature.spec.md) 
> 提示：本文件永远保持与生产环境代码逻辑同步。
* {{当前版本和发布日期，如：v1.0.0 2020-01-01，以及版本号的增加逻辑，不指定则为V+N}}

## 1. 功能业务概述 (Overview)
* **{{当前功能解决的核心业务问题及用户使用场景描述}}** 

## 2. 对外接口与规范 (API & Contracts)
### 接口契约
* **{{接口名称或功能，如：用户登录}}**
  *  `{{通讯协议，如：HTTP/POST或MQ/Topic}}` `{{API 相对路径或其他通信地址，如 /api/v1/auth/login}}` 
  * **请求参数 (Request)：**
    ```json
    {{全量最新请求参数 JSON 示例或 Schema 说明}}
    ```
  * **响应参数 (Response)：**
    ```json
    {{全量最新成功/失败响应参数 JSON 示例}}
    ```
......

## 3. 最新实现原理与代码位置
| 模块/层次 | 文件路径 | 职责描述 |
| :--- | :--- | :--- |
| {{控制层/入口层}} | {{项目根路径的相对文件地址路径}} | {{接收请求与参数校验职责说明}} |
| {{业务层/逻辑层}} | {{项目根路径的相对文件地址路径}} | {{核心业务逻辑与规则校验实现说明}} |
| {{仓库层/持久层}} | {{项目根路径的相对文件地址路径}} | {{数据库交互与持久化说明}} |
......

## 4. 当前功能详细设计逻辑
> 提示：功能的详细设计逻辑（包括但不限时序图、流程图、状态图、SVG图）和边界条件、异常处理等。

**{{每一个具体的原子功能详细设计逻辑说明,如果当前功能过大，可拆分为多个子功能文件，此处引入 `feature.{{子功能}}.md` 即可}}**
......

## 5. 核心验收标准 (Acceptance Criteria)
> 提示：每一个功能点的具体、可验证的通过指标。

* **AC-1:** {{具体的业务边界或防错校验指标 1（例如：密码错误超过 5 次锁定账户）}}
* **AC-2:** {{具体的业务边界或防错校验指标 2（例如：非法的邮箱格式必须返回 400 错误码）}}
......