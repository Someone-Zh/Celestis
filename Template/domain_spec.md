# {{Domain名称}} 领域规范 (domain.spec.md)

## 1. 领域现状视图
> 提示：此处记录该领域当前的全局技术骨架。若有模型变更，需在此同步更新。

### 涉及的模型与实体
| 实体描述 | 代码位置 | 核心表结构/缓存关联说明 |
| :--- | :--- | :--- |
| {{每一个核心通用模型的描述}} | {{项目根路径的相对文件地址路径}} | {{结构说明}} |
......

### 领域公共依赖
* **内部领域依赖 (Domains):** {{对应依赖领域的全局规范文件路径，如 `.spec/{{依赖Domain}}/domain.spec.md`}}
......
* **三方外部服务 (Services):** {{每一个依赖的外部服务/三方 API 全路径或供应商描述}}
......

---

## 2. 下属特性索引 (Feature Index)
> 提示 AI：若要查看或修改某个具体功能的完整逻辑，请直接加载对应的特性全量规范文件。

* **{{Feature_1名称}}** -> [查看最新全量设计]({{相对路径，如 `features/{{Feature_1}}/feature.spec.md`}})
* **{{Feature_2名称}}** -> [查看最新全量设计]({{相对路径，如 `features/{{Feature_2}}/feature.spec.md`}})
......

---

## 3. 领域核心业务规则与约束
> 提示：本领域下所有子特性必须无条件遵守的通用原子规则。

* {{该领域下必须严格遵守的架构规则、安全性要求或通用开发规约 1}}
* {{该领域下必须严格遵守的架构规则、安全性要求或通用开发规约 2}}
......