# 项目结构规范

## 1. 整体目录结构
> **规范说明**：本节由初始化脚手架自动扫描生成，禁止手动随意调整核心骨架。

```
{{项目/服务根目录}}/
├── .spec/                          # 规范根目录
├── {{核心源代码目录}}/               # 业务代码主目录
│   ├── {{全局共享/公共模块}}/         # 共享工具、常量、异常定义
│   └── {{领域/业务模块}}/             # 业务核心实现
└── {{配置及部署目录}}/               # 配置文件、脚本、Dockerfile等
......
```

## 2. 模块职责矩阵
| 模块/目录路径 | 核心职责 | 允许引入的依赖 | 严禁包含的内容 |
| :--- | :--- | :--- | :--- |
| `{{模块路径1}}` | `{{核心职责描述}}` | `{{允许依赖的库/模块}}` | `{{反模式/禁带内容}}` |
| `{{模块路径2}}` | `{{核心职责描述}}` | `{{允许依赖的库/模块}}` | `{{反模式/禁带内容}}` |
......


## 3. 核心要素速查表
| 分类 | 关键要素描述 | 物理物理路径/位置 | 备注/负责人 |
| :--- | :--- | :--- | :--- |
| **服务入口** | {{例如：应用启动类、Main函数、CLI入口}} | `{{具体文件路径}}` | |
| **对外API** | {{例如：HTTP Router、RPC服务定义、GraphQL Schema}} | `{{具体文件/目录路径}}` | |
| **配置文件** | {{例如：环境配置、应用参数、日志配置}} | `{{具体文件路径}}` | |
| **三方集成** | {{例如：消息队列监听器、定时任务、第三方SDK初始化}} | `{{具体文件/目录路径}}` | |
| {{每一个重要功能的描述比如服务入口点、配置文件、对外的Api（接口、服务）、业务逻辑等功能}}  |  {{功能描述}} | {{功能所在位置}} | {{备注/负责人}}
......


## 4. 标准数据流向
```mermaid
graph TD
{{    Client[用户/客户端请求] --> Controller[Controller/Entry层: 入口校验与路由]
    Controller --> Service[Service/Domain层: 核心业务逻辑]
    Service --> Infra[Infra/Repository层: 基础设施与持久化]
    Infra --> DB[(数据库/外部存储)]

    DB -.->|Return Entity| Infra
    Infra -.->|Return DTO/Model| Service
    Service -.->|Return VO/Result| Controller
    Controller -.->|HTTP/RPC Response| Client
    }}
```
> **流向约束**：
> 1. {{禁止从 `Controller` 层直接绕过 `Service` 层调用 `Infra` 层。}}
> 2. {{`Entity`（数据库实体）严禁穿透到 `Controller` 层，必须在 `Service` 层完成向 `VO/DTO` 的转换。}}
......


## 5. 模块依赖拓扑
- **{{上层依赖下层}}**：{{`{{模块A}}` -> `{{模块B}}` -> `{{模块C}}`}}
- **{{单向依赖原则}}**：{{严禁在 `{{模块X}}` 与 `{{模块Y}}` 之间引入循环依赖（Circular Dependency）。}}
- **{{三方依赖合规}}**：{{所有新增的三方依赖必须在 `.spec/rules.md` 中进行框架合规报备。}}
- **{{模块依赖原则}}**：{{具体规则}}
......



## 项目下功能 
| 功能 | 位置 | 描述 |
|------|------|------|
|{{功能信息（如用户管理）}}|{{功能}}/spec.md|{{对当前功能的详细描述比如都有哪些能力,下属还有哪些子功能}}
......

## 6. 业务领域（Domain）与特性（Feature）索引
> **映射说明**：本节严格对应 `.spec/{{Domain}}/` 目录下的业务拆分。

| 业务领域 (Domain) | 包含的核心特性 (Feature) | 领域规范路径 | 职责概述 |
| :--- | :--- | :--- | :--- |
| `{{DomainName1}}` | `{{Feature1}}`, `{{Feature2}}` | `.spec/{{DomainName1}}/domain.spec.md` | `{{该领域的核心业务边界能力描述}}` |
| `{{DomainName2}}` | `{{Feature3}}` | `.spec/{{DomainName2}}/domain.spec.md` | `{{该领域的核心业务边界能力描述}}` |
......