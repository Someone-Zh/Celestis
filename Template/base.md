## 附录中的文件模板中`......`代表的是可能有多个的意思
## 所有`{{}}` 包裹的都是需要替换的变量
## **必须遵守的约束**
- **必须**严格按照附录的文件格式生成 
- 使用中文编写
- 格式清晰，使用Markdown标题和代码块
- 当目标文件格式和给定的模板格式出现出入时需要修改其符合模板内容格式（需改目标文件内容格式时需保证信息不能缺失）
## **禁止行为**
- 不得虚构不存在的信息
