---
description: 归档需求
---
@Template/file_spec.md

@Template/base_root.md

## 必须给定一个已存在的需求目录，否则**必须**主动询问获取才能继续
## 归档需求
### 1. 在当前需求目录下分析`changes/tasks.md` 文件和 `design.md` 文件,根据内容更新对应功能的规范文件(`.spec/{{功能}}/spec.md`)
* 如果不存在则参考附录中的模板文件生成
* 在对应功能的规范文件(`.spec/{{功能}}/spec.md`)下移除`正在处理的需求目录`下对应需求

### 2. 在`.spec/spec.md` 尾部查找`项目下功能 `下是否有对应的功能.
* 没有则根据当前功能创建
* 仅包含 `{{功能规范文件}}` 如 `.spec/{{功能}}/spec.md ` 不要有下级目录和下级目录的文件

### 3. 更新所属功能的规范文件(`.spec/{{功能}}/spec.md`)中的 `当前功能详细信息` 下的对应设计文档路径。
* 如果本次需求有对`当前功能详细信息` 下的某些修改需要修改对应的设计文档使其保持最新的设计，但**必须**仅修改和此次有关的。
* 如果本次需求有还未在`当前功能详细信息` 下的记录的功能需求信息则在其中新增这部分记录，将相关设计抽离出根据功能在同级的 `.archive` 目录下创建对对应目录和设计文档（比如新增了鉴权的登录则`.spec/auth/.archive/login/design.md`).
* 必须秉承有则更新无则新建的理念使其拥有全部的最细的设计信息
* 可以多个功能需求对应一个设计文档，所以尽量不要过于拆分原始的设计文档，仅在对其他功能设计有修改时，或本次需求改动太过于庞大导致改动涉及太多的其他功能设计时进行拆分。
* 对历史需求完全重构时则直接指向新的设计文档即可（须完整的评估是否覆盖了历史全部设计，不然还是需要融合）。
  
### 4. 将当前`{{需求}}` 文件夹移入 同级的 `.archive` 目录下
* 出现同名则在名称后加下划线和递增数字如 `{{需求}}_1`

## 附录
* 功能目录下的spec.md 的格式
```{{功能}}/spec.md
@Template/function_spec.md
```
